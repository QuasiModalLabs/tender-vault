# Core retrieval
chromadb>=1.0,<2
sentence-transformers>=2.2.2
pandas>=2.0.0
requests>=2.31.0
pyyaml>=6.0

# MCP server — optional, only needed if using Claude Desktop
# Install with: pip install "mcp[cli]"
mcp>=2.0.0,<3

# LLM value extraction — optional, only used by `scripts/ingest --extract-values`
# (requires ANTHROPIC_API_KEY; default ingest path needs no credentials)
anthropic>=0.40.0,<1

# Text extraction for tender documents the user drops in by hand — one line per
# format registered in scripts/attachments.py's dispatch table.
#
# Optional in the same sense as mcp/anthropic: attachments.py imports and runs
# without either of these, and reports the miss per-file rather than failing.
# They are listed here because CI installs from this file and nothing else, so
# these lines are what give the extractors a runtime at all.
#
# openpyxl reads workbooks WITHOUT flattening them — every value keeps its
# sheet, column letter and row number. See the _extract_xlsx docstring for what
# it still cannot represent, particularly the cached-value trap that makes an
# uncalculated pricing column read as blank.
pdfplumber>=0.11,<1
python-docx>=1.1,<2
openpyxl>=3.1,<4

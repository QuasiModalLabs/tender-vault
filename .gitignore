# Python
__pycache__/
*.py[cod]
.venv/
venv/

# CSV cache — re-downloaded when you run ingest
.cache/

# ChromaDB — rebuilt from scratch by ingest.py or the weekly Actions run.
# Not committed, to keep the repo small. Anyone cloning runs ingest.py once.
chroma_db/

# NOTE: data/contracts.db IS committed on purpose. It's a small filtered
# SQLite derived from the Open Government Licence contracts dataset,
# refreshed quarterly by Actions, and part of what this repo publishes.

# Sampling runs (--source, --max-audits) write here instead of over the
# committed databases, and half-finished builds land in .part / .old.
*.sample.db
*.db.part
chroma_db.sample/
chroma_db.old/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# The vault IS committed — that's the whole point of this project.
# The digests directory accumulates one markdown file per week from Actions.

vault/.obsidian/
